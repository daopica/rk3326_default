```yaml
name: RK3326 U-Boot Build

on:
  workflow_dispatch:

jobs:
  build:
    name: Build RK3326 U-Boot
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Show environment
        run: |
          echo "================================"
          echo "Ubuntu"
          echo "================================"
          cat /etc/os-release

          echo
          echo "================================"
          echo "Repository"
          echo "================================"
          ls -lah
          echo
          ls -lah u-boot
          echo
          ls -lah rkbin

      - name: Test
        run: |
          echo "GitHub Actions is working."
          echo "RK3326 build environment test passed."
```
